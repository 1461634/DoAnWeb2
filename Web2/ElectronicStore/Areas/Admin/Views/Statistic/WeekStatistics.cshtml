@model PagedList.IPagedList<ElectronicStoreConnection.View_WeekStatistic>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Sales Statistics By Week";
}
<div class="panel panel-success">
    <div class="panel-heading">
        @Html.PagedListPager(Model, page => Url.Action("WeekStatistics", new { page = page }), PagedListRenderOptions.TwitterBootstrapPagerAligned)
    </div>
    <div class="panel-body">
        <table class="table table-responsive table-hover table-striped">
            <thead>
                <tr>
                    <th>
                        Fiscal Week
                    </th>
                    <th>Revenue</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.WeekNumber</td>
                        <td>@item.Revenue</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

