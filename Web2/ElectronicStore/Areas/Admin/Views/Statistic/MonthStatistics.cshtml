@model PagedList.IPagedList<ElectronicStoreConnection.View_MonthStatistic>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Sales Statistics By Month";
}
<div class="panel panel-success">
    <div class="panel-heading">
        @Html.PagedListPager(Model, page => Url.Action("DayStatistics", new { page = page }), PagedListRenderOptions.TwitterBootstrapPager)
    </div>
    <div class="panel-body">
        <table class="table table-responsive table-hover table-striped">
            <thead>
                <tr>
                    <th>
                        Fisal Year
                    </th>
                    <th>Fiscal Month</th>
                    <th>Revenue</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Year</td>
                        <td>@item.Month</td>
                        <td>@item.Revenue</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

